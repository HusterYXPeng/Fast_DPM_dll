两个文件分别代表两个工程。一个为生成封装了DFM算法的dll的工程，一个为调用dll的工程。

1、DPM_dll：
（1）具体的情况见release文件夹下的“说明.txt”文件
（2）在工程中，设置不同的模型，可以检测不同的目标。

2、FastDPM-2015-Nov - dll
（1）编译这个工程需要配置opencv249才可以生成dll
（2）将工程的属性设置成dll，生成dll； 将工程的属性设置成exe，可以得到这个dll独立运行需要运行的exe。
